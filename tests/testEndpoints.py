import unittest
from ireportermodels import app
import json

class TestusersData(unittest.TestCase):
	def setUp(self):
         self.test_client = app.test_client()
        self.assertEqual(users_Data)
